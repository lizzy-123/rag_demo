完整分阶段开发顺序（按依赖由浅到深，避免一次性加载过多逻辑卡顿）
当前已完成：目录骨架 + config 配置类 + mcp_client_adapter 三层封装 + 模块说明文档
## 阶段 1：文件 IO 与基础存储工具（最低依赖，优先开发）
开发目标
实现本地文件读写、URL 历史记录、黑白名单加载、原始 MD 保存逻辑，支撑后续抓取流程
下发指令参考
plaintext
基于现有ai_knowledge_crawler_mcp目录与config、mcp_client_adapter代码，开发crawl_task内基础文件工具模块：
1. 实现raw_source_md目录按「日期/分类」创建文件夹、写入原始Markdown文件；
2. 实现历史抓取URL持久化存储（json文件记录已爬链接，用于增量过滤）；
3. 读取config内黑名单域名、分领域搜索关键词；
仅完成文件IO、配置读取、URL去重缓存逻辑，不实现MCP批量调用、清洗分片、定时任务。
产出文件
crawl_task/file_storage.py、crawl_task/keyword_loader.py
## 阶段 2：增量采集流水线（搜索 + 批量抓取，纯素材获取）
开发目标
串联必应搜索 MCP + Fetch 抓取 MCP，完成「关键词搜索→过滤旧 URL→批量抓取页面→存入 raw_source_md」完整素材采集链路
下发指令参考
plaintext
在crawl_task中实现素材采集流水线：
1. 读取配置多领域关键词，调用bing_search_adapter获取最新文章URL；
2. 用本地历史URL缓存过滤已抓取链接、黑名单域名，仅保留增量新链接；
3. 批量调用fetch_adapter抓取网页纯净MD原文；
4. 自动将原文+来源元数据写入raw_source_md对应分类日期文件夹；
仅实现单次手动采集逻辑，暂不接入文档清洗、RAG分片、定时调度。
产出文件
crawl_task/crawl_pipeline.py，提供单次采集对外函数
## 阶段 3：文本预处理流水线（清洗、去重、RAG 分片核心）
开发目标
对接doc_processor_adapter，完成原始 MD 清洗、语义去重、文本分块、自动打标签、双目录归档
下发指令参考
plaintext
开发crawl_task下预处理流水线逻辑：
1. 批量读取raw_source_md新增原始MD文件；
2. 调用doc_processor_adapter执行文本清洗、全文+语义双层去重、按配置token长度分片；
3. 自动生成技术分类标签、段落摘要、来源元数据；
4. 完整无分片标准MD存入processed_knowledge长期知识库；
5. 分片结构化JSON输出至rag_export目录，字段兼容LangChain Document；
暂不实现定时、单元测试，仅提供手动预处理入口。
产出文件
crawl_task/process_pipeline.py
## 阶段 4：统一入口 entry.py + LangChain RAG 对接函数
开发目标
封装对外调用接口，提供一键执行「采集 + 预处理」完整流程，新增读取 rag_export 分片转 LangChain Document 工具
下发指令参考
plaintext
完善模块根目录entry.py统一入口：
1. 封装单次完整执行函数：执行搜索抓取 + 预处理归档全链路；
2. 封装独立分步函数：仅采集、仅预处理；
3. 新增RAG加载工具函数：读取rag_export分片JSON，直接转换为LangChain标准Document对象，支持增量加载新分片；
可外部导入entry模块调用全部能力，不新增定时、测试逻辑。
## 阶段 5：定时调度能力（APScheduler 后台循环）
开发目标
增加后台定时任务，支持自定义小时间隔，自动周期性执行全采集预处理流程
下发指令参考
plaintext
为crawl_task新增定时调度模块，基于APScheduler实现：
1. 读取config配置的抓取间隔小时数；
2. 后台循环自动执行完整采集+预处理流水线；
3. 提供入口开启/关闭定时任务；
4. 所有执行日志写入logs目录持久化；
暂不编写单元测试，仅实现调度运行逻辑。
产出文件
crawl_task/scheduler.py
## 阶段 6：TDD 单元测试 + 全量校验收尾（启用 Superpowers）
开发目标
配套全链路单元测试，完工校验所有约束，输出完整启动文档
下发指令参考
plaintext
启用superpowers TDD测试驱动流程，为模块所有核心函数编写单元测试：MCP适配器调用、文件存储、采集流水线、预处理分片、RAG加载；
1. 在ai_knowledge_crawler_mcp内新建tests测试目录；
2. 覆盖正常流程、重复URL、抓取失败、超长文本分片等边界用例；
3. 完工执行全量约束校验：确认未修改项目原有代码、MCP通信全部复用agent/mcp_client；
4. 输出两种启动方式：手动单次执行命令、后台定时执行命令；
5. 同步更新ai_knowledge_crawler_mcp/模块说明.md，补充测试、启动说明。
可选扩展阶段（主线全部完成后按需添加）
英文论文采集分支：新增 Jina-AI-MCP-Tools 适配器调用逻辑，单独配置英文关键词抓取海外 AI 文档；
向量库入库扩展：封装 supabase-mcp 调用，读取 rag_export 分片自动生成向量写入 Supabase；
运维辅助脚本：全量知识库去重清理、失败抓取链接重试工具。
关键开发约束提醒
每一段指令下发前都可以执行 >/permissions pre-approve edit bash 预授权，避免弹窗中断流程；
全程只在ai_knowledge_crawler_mcp内部生成 / 修改文件，绝不触碰 agent、skills、sub_agents 等原有目录代码。

开发完成后同步更新 ai_knowledge_crawler_mcp / 模块说明.md，补充新增文件功能说明。
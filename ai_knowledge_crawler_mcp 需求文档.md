# 前置执行指令（给Claude Code）
1. 先完整阅读本文档全部需求，严格按照文档规范开发；
2. 仅在 langchain_rag_team/ 下新建文件夹 ai_knowledge_crawler_mcp，所有代码、目录仅生成在该文件夹内部；
3. 严禁修改、新增、删除、注释项目内其他目录代码：agent/、sub_agents/、skills/、tests/ 全部原有文件禁止改动；
4. MCP协议底层通信、握手、报文收发、连接逻辑全部复用 agent/mcp_client.py 里现有封装的客户端类，本模块只做业务参数封装，绝不重复实现MCP底层协议；
5. 开发完成后输出：目录结构说明 + 每个文件的功能说明 + 运行启动方式；
6. 适配WSL Ubuntu 22.04环境，代码兼容现有LangChain项目，输出分片JSON可直接转为LangChain Document对接现有RAG。
7. 

# 开发需求文档：AI 知识库 MCP 采集预处理模块

## 一、项目基础信息

- **开发工具**：Claude Code
- **运行环境**：WSL Ubuntu 22.04
- **项目根路径**：`code/langchain_rag_team/`
- **现有基础能力**：
  - 项目 `agent/mcp_client.py` 已封装 MCP 协议通信，可直接调用 MCP 广场 Hosted 类型服务；
  - 项目基于 LangChain 搭建私有 RAG 系统，已有向量库加载、知识库问答逻辑。
- **开发约束**：本次新增功能完全独立，**不得修改原有 `agent`、`sub_agents`、`skills` 目录下任意业务代码**。

## 二、建设目标

在 `langchain_rag_team/` 根目录新建独立模块文件夹 `ai_knowledge_crawler_mcp`，搭建一套自动化 AI 领域素材增量采集流水线：

- 通过 MCP 广场封装工具完成全网技术内容爬取、文本清洗、重复内容过滤、长文本 RAG 分片、本地 MD 归档；
- 最终产出标准化分片数据，无缝对接项目现有 LangChain RAG；
- 实现私有 AI 知识库定时自动更新。

## 三、必须使用的 MCP 广场 Hosted 服务

### 核心 3 个（强制使用）

1. **必应搜索中文 MCP**（搜索工具分类）
   - **能力**：分领域批量搜索 AI 技术关键词，限定近 N 天增量内容，输出文章标题、发布时间、原文链接；自动过滤历史已抓取链接，只保留新素材 URL。
   - **检索分类关键词**：`RAG`、`大模型基础`、`Multi-Agent 智能体`、`Vibe Coding`、`大模型评测`

2. **Fetch 网页内容抓取 MCP**（浏览器自动化分类）
   - **能力**：批量输入 URL，自动渲染静态 / JS 动态页面，剔除广告、导航栏、侧边栏，输出纯净文章 Markdown 原文。

3. **智能体文档处理 MCP**（知识管理与记忆分类，预处理核心）
   - 输入原始 MD 批量执行全量预处理：
     1. 文本清洗：过滤乱码、无效换行、无关推荐内容；
     2. 双层去重：全文哈希精准去重 + 语义相似度段落去重；
     3. RAG 分片：按可配置 token 长度拆分长文本，适配向量检索；
     4. 元数据生成：自动标记技术分类标签、文章摘要、原文链接、抓取时间；
     5. 输出两种产物：完整归档 MD、结构化分片 JSON。

### 可选扩展 MCP（按需做分支逻辑）

- **Jina-AI-MCP-Tools**：专门抓取、解析海外英文 AI 论文、海外技术博客，适配英文知识库采集分支。

## 四、模块目录结构规范（固定不可调整）
    langchain_rag_team/
    └── ai_knowledge_crawler_mcp/
    ├── mcp_client_adapter/ # MCP服务业务封装层，复用项目原有mcp_client通信
    ├── crawl_task/ # 任务调度、定时逻辑、关键词管理、流程编排
    ├── raw_source_md/ # 原始未清洗抓取MD，按「日期/技术分类」分文件夹存储
    ├── processed_knowledge/ # 清洗去重完成后的标准归档MD，作为本地知识库
    ├── rag_export/ # RAG对接层：分片结构化JSON，兼容LangChain Document
    ├── config/ # 全局配置：定时间隔、分片长度、搜索关键词、黑名单域名
    ├── logs/ # 全流程日志：抓取失败日志、重复命中日志、MCP调用日志
    └── entry.py # 统一入口：支持手动单次执行 / 后台定时循环执行

## 五、全链路业务流程需求

1. **任务触发**：两种模式
   - ① 手动单次执行；
   - ② 后台定时自动执行，可自定义抓取间隔小时数。

2. **增量搜索采集**
   - 读取 `config` 内分领域关键词，调用必应搜索 MCP；
   - 对比本地历史抓取记录，过滤已爬取 URL、黑名单站点，仅保留新增待爬链接。

3. **网页批量抓取**
   - 将增量 URL 批量传入 Fetch MCP，抓取纯净 Markdown 原文；
   - 原始文件存入 `raw_source_md` 目录，同步记录来源、抓取时间元数据；
   - 抓取失败链接写入日志，自动重试 2 次。

4. **批量预处理（核心）**
   - 将 `raw_source_md` 内全部新增原始 MD 批量送入智能体文档处理 MCP；
   - 一次性完成清洗、去重、文本分片、自动分类打标签。

5. **分层存储落地**
   - 完整无分片标准 MD → 存入 `processed_knowledge`（长期本地知识库归档）；
   - 带元数据、分段文本的结构化 JSON → 存入 `rag_export`（给 RAG 使用）。

6. **对接现有 LangChain RAG**
   - 在模块内封装工具函数，读取 `rag_export` 下分片 JSON，直接转换为 LangChain 标准 `Document` 对象；
   - 支持增量加载，仅读取本次新生成分片，追加写入向量库，无需全量重建知识库。

## 六、附加功能需求

- **日志完整留存**：记录每条 URL 抓取状态、重复文章数量、MCP 调用耗时、异常报错详情。
- **容错机制**：MCP 接口超时、页面 404、解析失败不阻断整体流程，失败内容单独归档日志。
- **独立运行能力**：模块可单独启动执行采集任务，也可在项目根 `main.py` 导入 `entry.py` 统一调度。
- **英文素材扩展分支**：可选开启 Jina MCP 分支，单独配置英文关键词，采集海外 AI 论文、开源项目文档。

## 七、硬性开发约束

1. 所有代码仅存放于 `ai_knowledge_crawler_mcp` 内部，**禁止修改项目其他目录代码**。
2. MCP 通信逻辑复用 `agent/mcp_client.py`，**不重复实现 MCP 底层协议**。
3. 文件存储严格按照目录分层规范，统一 MD 头部元数据格式，便于后续检索。
4. 适配 WSL Ubuntu 文件读写、定时任务运行环境，无系统兼容性问题。

## 八、硬性红线约束
- 除 langchain_rag_team/ai_knowledge_crawler_mcp/ 内新增文件外，项目其余所有已有文件不允许做任何修改。
# Changelog

本文档记录项目的所有显著变更，格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [0.2.0] - 2026-05-07

### 新增

#### 功能优化
- **批量文档加载**：自动扫描 `team_doc` 目录下所有支持文件，支持递归子目录加载，无需手动指定文件路径。
- **支持更多文件类型**：新增 `.md`、`.xlsx`、`.pptx`，保留 `.pdf` / `.txt` / `.docx`，可轻松扩展（如 `.csv`、`.epub`）。
- **历史对话记忆**：本地 JSON 持久化存储，重启不丢失；自动限制最大轮数，避免上下文过长；支持查看/清空历史。
- **自定义 Prompt 模版**：内置专业默认模版，支持用户实时输入自定义 Prompt。
- **对话收藏功能**：输入 `collect` 命令即可收藏优质回答，收藏内容独立持久化存储。

#### 性能优化
- **自动判断向量库是否存在**：启动自动检测，已存在直接加载（无需注释代码）。首次构建，后续秒启动。
- **分块参数智能调整**：根据文档平均长度自动计算最优分块大小（长文档大分块、短文档小分块）。
- **缓存机制**：检索结果 LRU 缓存，相同问题直接返回，大幅提升重复提问速度。
- **异步处理**：全异步文档加载，多文件并行处理，解决大文件加载卡顿问题。

#### 检索效果优化
- **混合检索（FAISS 向量 + BM25 关键词）**：向量检索负责语义匹配，BM25 检索负责关键词匹配，加权融合，显著提升召回率和准确率，可通过配置开关控制。

#### 架构优化
- **面向对象重构**：模块化设计（模型、文档、向量库、对话、RAG 链完全解耦），易于维护、扩展、测试。
- **稳定性增强**：全局异常捕获，文件操作容错处理，配置集中管理，数据结构标准化。
- **可扩展性**：轻松切换向量库（Chroma / Pinecone 等），轻松添加新模型/新文件类型，插件式架构。

### 使用说明变更
- 安装依赖命令更新：需安装 `langchain-community`、`unstructured`、`openpyxl`、`python-pptx` 等包。
- 流程简化为：创建 `.env` 文件 → 将所有文档放入 `team_doc` → 运行代码自动加载。
- 交互命令新增：`help`、`collect`、`prompt`、`history`、`clear`。

## [0.1.1] - 2026-05-06

### 功能特点（初始版本）
- 支持多种文档格式：PDF、TXT、DOCX
- 使用智谱 AI 的 `glm-4-flash` 大语言模型和 `embedding-2` 嵌入模型
- 向量检索 + LLM 生成，答案可溯源（返回引用片段）
- 可配置的分块参数和检索数量
- 环境变量切换模型（预留 OpenAI 配置）

### 基础功能
- 单文档加载与问答
- 命令：`q` 退出

## [0.1.0] - 2026-05-05

### 初始版本
- 项目基础框架搭建
- 支持通过环境变量配置 API 密钥
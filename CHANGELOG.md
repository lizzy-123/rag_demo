# Changelog

本文档记录项目的所有显著变更，格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [0.3.0] - 2026-05-08

### 新增

#### 智能文档变更管理
- **文件级变更检测**：自动记录每个文件的修改时间（mtime）和大小，启动时对比当前目录，精准识别“新增”、“修改”、“删除”文件。
- **智能更新策略**：
  - 仅新增文件 → 仅对新文件进行加载、分块、向量化，并**增量添加**到现有 FAISS 索引（无需全量重建），极大缩短更新耗时。
  - 存在修改或删除文件 → 自动触发**全量重建**，确保向量库与真实文档绝对一致，避免旧数据残留。
- **元数据持久化**：将文件状态信息存储在 `faiss_team_vector_db/file_metadata.json` 中，重启程序不丢失。

#### 文档内容噪音清洗
- **内置通用噪音过滤器**：自动移除文档中的常见干扰文本：
  - ❌ QQ 群号码及广告行
  - ❌ URL（http/https/www）
  - ❌ PDF 水印（如 `Evaluation Warning`、`Spire.PDF`）
  - ❌ 页眉页脚页码（如“第 2 页 共 2 页”）
  - ❌ 单独的数字行
  - ❌ 多余空行（连续换行合并为最多两个）
- **清洗即加载**：在文档加载后立即清洗，保证索引和检索内容干净，同时保留原始文档不受影响。

#### 向量库与检索器增强
- **FAISS 增量添加支持**：`DocumentAwareFAISSRetriever` 包装类新增 `add_documents` 方法，使得 FAISS 支持动态追加向量。
- **语料库及指纹文件持久化**：
  - `corpus.json`：保存所有文档块原文，用于重建 BM25 检索器。
  - `fingerprints.json`：保存每个文档块的哈希指纹，用于去重检测。
- **BM25 动态重建**：每次增量添加后，基于完整语料文件重建 BM25 检索器（保证混合检索的准确性）。

### 改进

- **路径处理健壮性**：所有路径统一转为绝对路径，彻底避免 `relative_to` 跨平台报错。
- **代码结构优化**：将噪音清洗逻辑独立为 `TextCleaner` 类，方便扩展与测试。
- **日志输出细化**：启动时明确提示当前执行的是“全量重建”、“增量添加”还是“直接加载”，提升可观测性。

### 修复

- 修复了旧版向量库缺少 `corpus.json` 或 `fingerprints.json` 时，程序无法正确重建 BM25 检索器的问题。
- 修复了相对路径与绝对路径混合使用导致的 `ValueError: '...' is not in the subpath of ...` 错误。
- 修复了 `EnsembleRetriever` 调用 `get_relevant_documents` 报错的问题（统一改用 `invoke` 方法）。

### 性能提升

- **增量添加**：当只有新增文件时，避免全量重建，速度从分钟级降至秒级（取决于新增文档大小）。
- **文件元数据扫描**：轻量级文件系统操作，开销可忽略。
- **BM25 重建**：虽然每次增量添加仍需全量重建 BM25，但语料文件读写高效，对中小规模文档库影响有限。

### 依赖变更

- 新增 `rank_bm25` 依赖（BM25 检索器所需）。运行 `uv add rank_bm25` 或 `pip install rank_bm25` 安装。

### 升级指南

- **从 0.2.x 升级**：首次运行 0.3.0 时，程序会自动检测到缺少 `file_metadata.json` 等文件，并执行一次**全量重建**（耗时较长，仅一次）。之后即可享受增量更新能力。
- 建议备份原有的 `faiss_team_vector_db` 目录，以防需要回退。
- 如需自定义噪音清洗规则，修改 `TextCleaner.clean` 方法中的正则表达式即可。


## [0.2.0] - 2026-05-07

### 新增

#### 功能优化
- **批量文档加载**：自动扫描 `team_doc` 目录下所有支持文件，支持递归子目录加载，无需手动指定文件路径。
- **支持更多文件类型**：新增 `.md`、`.xlsx`、`.pptx`，保留 `.pdf` / `.txt` / `.docx`，可轻松扩展（如 `.csv`、`.epub`）。
- **历史对话记忆**：本地 JSON 持久化存储，重启不丢失；自动限制最大轮数，避免上下文过长；支持查看/清空历史。
- **自定义 Prompt 模版**：内置专业默认模版，支持用户实时输入自定义 Prompt。
- **对话收藏功能**：输入 `collect` 命令即可收藏优质回答，收藏内容独立持久化存储。

#### 性能优化
- **自动判断向量库是否存在**：启动自动检测，已存在直接加载（无需注释代码）。首次构建，后续秒启动。
- **分块参数智能调整**：根据文档平均长度自动计算最优分块大小（长文档大分块、短文档小分块）。
- **缓存机制**：检索结果 LRU 缓存，相同问题直接返回，大幅提升重复提问速度。
- **异步处理**：全异步文档加载，多文件并行处理，解决大文件加载卡顿问题。

#### 检索效果优化
- **混合检索（FAISS 向量 + BM25 关键词）**：向量检索负责语义匹配，BM25 检索负责关键词匹配，加权融合，显著提升召回率和准确率，可通过配置开关控制。

#### 架构优化
- **面向对象重构**：模块化设计（模型、文档、向量库、对话、RAG 链完全解耦），易于维护、扩展、测试。
- **稳定性增强**：全局异常捕获，文件操作容错处理，配置集中管理，数据结构标准化。
- **可扩展性**：轻松切换向量库（Chroma / Pinecone 等），轻松添加新模型/新文件类型，插件式架构。

### 使用说明变更
- 安装依赖命令更新：需安装 `langchain-community`、`unstructured`、`openpyxl`、`python-pptx` 等包。
- 流程简化为：创建 `.env` 文件 → 将所有文档放入 `team_doc` → 运行代码自动加载。
- 交互命令新增：`help`、`collect`、`prompt`、`history`、`clear`。

## [0.1.1] - 2026-05-06

### 功能特点（初始版本）
- 支持多种文档格式：PDF、TXT、DOCX
- 使用智谱 AI 的 `glm-4-flash` 大语言模型和 `embedding-2` 嵌入模型
- 向量检索 + LLM 生成，答案可溯源（返回引用片段）
- 可配置的分块参数和检索数量
- 环境变量切换模型（预留 OpenAI 配置）

### 基础功能
- 单文档加载与问答
- 命令：`q` 退出

## [0.1.0] - 2026-05-05

### 初始版本
- 项目基础框架搭建
- 支持通过环境变量配置 API 密钥